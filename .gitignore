node_modules/
*.log
.DS_Store

# Release artifacts — semantic-release writes CHANGELOG.md and commits it itself.
.semrel/

# Runtime coordination state — live territory claims, never committed.
.harness/
