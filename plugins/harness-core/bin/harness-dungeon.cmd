@echo off
:: GENERATED by scripts/sync-launchers.mjs — edit that, not this.
:: Windows ignores a shebang, so the sibling shell launcher is unrunnable here.
if defined CLAUDE_PLUGIN_ROOT if exist "%CLAUDE_PLUGIN_ROOT%\lib\dungeon.mjs" (
  node "%CLAUDE_PLUGIN_ROOT%\lib\dungeon.mjs" %*
  exit /b %errorlevel%
)
node "%~dp0..\lib\dungeon.mjs" %*
exit /b %errorlevel%
